name: Закрытие старых issues

on:
  schedule:
    - cron: '0 0 * * *'  # Ежедневно в полночь
  workflow_dispatch:      # Ручной запуск

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - name: Закрыть неактивные issues и PR
        uses: actions/stale@v9
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

          # Issues
          stale-issue-message: |
            👋 Этот issue был автоматически помечен как неактивный, потому что в течение последних 60 дней не было никакой активности.

            Он будет закрыт через 7 дней, если не последует дальнейшей активности.
          close-issue-message: |
            🔒 Этот issue был автоматически закрыт из-за отсутствия активности.

            Если проблема всё ещё актуальна, пожалуйста, откройте новый issue.
          stale-issue-label: 'неактивный'
          days-before-issue-stale: 60
          days-before-issue-close: 7

          # Pull Requests
          stale-pr-message: |
            👋 Этот Pull Request был автоматически помечен как неактивный, потому что в течение последних 30 дней не было никакой активности.

            Он будет закрыт через 7 дней, если не последует дальнейшей активности.
          close-pr-message: |
            🔒 Этот Pull Request был автоматически закрыт из-за отсутствия активности.

            Если изменения всё ещё актуальны, пожалуйста, откройте новый PR.
          stale-pr-label: 'неактивный'
          days-before-pr-stale: 30
          days-before-pr-close: 7

          # Общие настройки
          exempt-issue-labels: 'pinned,enhancement,help wanted'
          exempt-pr-labels: 'pinned,work in progress'
